{
  "name": "klerk-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "klerk — Document Intelligence Assistant (TS workspaces root)",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "dev:cli": "pnpm --filter @yohnmaistre/klerk-cli dev",
    "build:cli": "pnpm --filter @yohnmaistre/klerk-cli build",
    "build:ext": "pnpm --filter @yohnmaistre/pi-extension-klerk build"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}
